{
  "skill_name": "skill-eval",
  "evals": [
    {
      "id": 1,
      "case_type": "control",
      "prompt": "Run skill-eval for the 'capture' skill. The skill's SKILL.md and evals/evals.json are both present. evals.json has 2 cases. Run the eval and produce the formatted report.",
      "expected_output": "The skill spawns an executor and grader subagent for each of the 2 cases, then produces a report using the exact format: '## Skill Eval: capture', case headers '### Case N: ...', per-expectation verdicts with evidence, and a Summary block with pass count and overall verdict.",
      "expectations": [
        "Report header is '## Skill Eval: capture'",
        "Each case has a '### Case N: <prompt truncated to 60 chars>' heading",
        "Each expectation is graded pass, fail, or partial with a one-line evidence citation",
        "Summary block shows '<n>/<total> expectations passed' and a 'Verdict: pass | fail' line"
      ]
    },
    {
      "id": 2,
      "case_type": "boundary",
      "prompt": "Run skill-eval for the 'my-skill' skill. SKILL.md exists but skills/my-skill/evals/evals.json does not exist.",
      "expected_output": "The skill runs the fallback evaluator, reports recommended scenarios, asks whether to write them as skills/my-skill/evals/evals.json, and stops if the user says no.",
      "expectations": [
        "Runs the fallback evaluator instead of normal eval cases",
        "Reports recommended scenarios to formalize as evals.json cases",
        "Asks whether to write skills/my-skill/evals/evals.json",
        "Stops without proceeding to Step 3 when the user says no"
      ]
    },
    {
      "id": 3,
      "case_type": "edge",
      "prompt": "Run skill-eval for a skill whose evals.json has the correct structure but zero eval cases in the 'evals' array.",
      "expected_output": "The skill reports that no test cases were found as a finding and stops, rather than reporting 0/0 passed.",
      "expectations": [
        "Reports that evals.json has no test cases",
        "Treats empty evals array as a finding",
        "Does not produce a passing verdict for zero cases"
      ]
    },
    {
      "id": 4,
      "case_type": "compliance",
      "prompt": "Run skill-eval for the 'context-check' skill. One eval case has an expectation: 'Writes to context-findings.md'. The executor's response describes checking imports and flagging issues but never mentions writing the file. Grade conservatively.",
      "expected_output": "The skill grades the file-write expectation as 'fail' or 'partial' (not 'pass') because the executor did not explicitly describe writing the file. The grader cites the absence of evidence.",
      "expectations": [
        "Grades the file-write expectation as fail or partial",
        "Does not grade it pass without explicit executor evidence",
        "Cites absence of evidence in the evidence line"
      ]
    },
    {
      "id": 5,
      "case_type": "self-check",
      "prompt": "Run skill-eval for the 'skill-eval' skill itself. The file skills/skill-eval/evals/evals.json exists and has authored cases.",
      "expected_output": "The skill checks its own evals/evals.json file, reports the authored eval count in the structural check, and proceeds through the normal executor+grader path instead of the fallback creation prompt.",
      "expectations": [
        "Checks skills/skill-eval/evals/evals.json during eval coverage",
        "Reports a passing eval count for the existing authored cases",
        "Proceeds to the normal executor+grader path",
        "Does not show the fallback write prompt when its own eval file exists"
      ]
    }
  ]
}
