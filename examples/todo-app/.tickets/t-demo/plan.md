# Plan - Build a simple Todo list

Ticket: `t-demo`

## Sprint Brief

Build a small dependency-free browser Todo app with localStorage persistence and Node tests for core behavior.

Files:

- `src/index.html`
- `src/styles.css`
- `src/app.js`
- `tests/todo.test.mjs`

Approved choices:

- Use `localStorage`.
- Provide `All`, `Open`, and `Done` filters.
- Keep the example framework-free so users can focus on canon's workflow.
