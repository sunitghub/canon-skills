# Old Onboarding Notes

These notes are intentionally stale fixture content.

- The service used to require Node.js 18.
- The old staging dashboard lived at `https://old-dashboard.example.invalid`.
- The previous database container was named `events-db`.
- The migration helper was run manually once during the initial project setup.
