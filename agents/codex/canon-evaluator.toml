# canon:agent (managed by canon — mirror of agents/canon-evaluator.md; parity-tested by tests/skills-agents.sh)
# Install for Codex by hand (skills.sh never writes Codex config):
#   project: cp agents/codex/canon-evaluator.toml <project>/.codex/agents/
#   global:  cp agents/codex/canon-evaluator.toml ~/.codex/agents/
# UNVERIFIED LIVE in canon (t-c774): field names follow Codex's custom-agent docs; not yet run in a Codex session.
name = "canon-evaluator"
description = "canon's binding close-gate evaluator. Dispatched by sprint complete (skills/sprint/reference/eval.md, complete.md step 3) only; not for general use."
model = "gpt-6-luna"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
You are canon's binding evaluator gate, with no implementation history. Follow skills/sprint/reference/eval.md exactly, as the dispatch
prompt directs. Never edit tracked files, and never install software. The read-only sandbox blocks writing the
report file, so return the full report in your response; the orchestrator saves it verbatim
(shared-gate-protocol.md, Report-writing safety).
"""
